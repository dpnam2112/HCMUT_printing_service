export default function Container({ children }) {
  // return <div className={"w-full px-6 md:px-40 lg:px-60 py-10"}>{children}</div>
  return <div className={"w-full mx-auto max-w-7xl px-6 lg:px-8"}>{children}</div>
}
